<%= form_with(model: order_album) do |form| %>
  <% if order_album.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_album.errors.count, "error") %> prohibited this order_album from being saved:</h2>

      <ul>
        <% order_album.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.text_field :order_id %>
  </div>

  <div>
    <%= form.label :album_copy_id, style: "display: block" %>
    <%= form.text_field :album_copy_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
